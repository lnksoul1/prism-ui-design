# Prism 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | Prism（npm 包名：`ui-design-mcp-server`） |
| 产品版本 | 1.1.0 |
| PRD 版本 | v1.1 |
| 更新日期 | 2026-08-16 |
| 状态 | 已实现主要能力，持续迭代中 |
| 上游依据 | `prism-product-definition-v2.md`（产品定义 v3.2）、`docs/archive/prism-nonprofessional-painpoints.md`、`docs/archive/prism-uiux-design-plan.md`、2026-08-16 产品决策修订 |

> **v1.1 修订摘要（2026-08-16）**
> - 删除自由画布/绘制功能，编辑与预览只保留 **流式预览** 与 **播放** 两个视图。
> - 删除组件库、设计系统库、交互模板（6 个）相关功能。
> - 新增/保留统一 **设计库**：24 个设计风格 + 117 个组件模板及其变体，支持拖拽添加与就地替换，并配套术语模板。
> - 导入来源由 5 类改为 **4 类**：项目文件夹 / 网页 URL / HTML 代码或文件 / 实际界面截图。
> - 明确 **每一个元素都具备全部编辑能力**。
> - 新增 **组件任意组合**：组件可嵌套、可拖拽加入其他组件；**内部元素可独立组件化**（设置组件类型/属性/外观）。

---

## 1. 项目概述

Prism 是一个**面向非设计专业用户的可视化 UI/UX 调整台**。用户把自己（或 AI）已经做出来的网站/App 导入 Prism，在流式预览视图中对任意元素进行精确、自由的调整，然后**一键应用回真实产物**。

Prism 不是一次性原型生成器：它不负责"首次生成"，而负责"精确修改"。用户想快，可以从设计库拖拽设计风格或组件模板快速变更；想细，可以选中任意元素直接编辑。内置 AI 只是可选的脚手架。

系统由三部分组成：

1. **MCP Server**：供 AI Agent / 开发者通过 stdio 或 Streamable HTTP 调用设计能力。
2. **Node 服务 + Web 控制台（Dashboard）**：内置 REST/WebSocket 服务与浏览器客户端，提供流式预览、播放、设计库、属性编辑等界面。
3. **Electron 桌面壳**：双击即用，自动启动本地服务并打开控制台，零命令行门槛。

---

## 2. 定位与价值主张

### 一句话定位

**Prism 是非设计专业用户的可视化调整台：导入自己创作或 AI 创作的网站/App → 在流式预览中精确、自由地调整任意元素 → 一键应用回真实产物。想快，用设计库的风格/组件模板快速变更；想细，每个元素都可以直接编辑。AI 生成只是可选的脚手架。**

### 核心心智

> **导入 = 开始；调整（或套设计库模板）= 核心价值；一键应用 = 闭环。**

### 价值主张

- **不破坏已有产物**：导入原始 HTML/URL/项目文件，保留原始 `<head>`、CSS、脚本与交互，一键应用从原始文档重建产物。
- **编辑体验为北极星**：每个元素（顶层组件与内部元素）都具备完整的编辑能力——移动、缩放、精确数值、多选、对齐/分布、Z 轴、图层重命名、标尺/参考线/吸附；内部元素可独立组件化；组件可任意组合嵌套。
- **非设计师友好**：自然语言指令引擎、可点击示例、"解读"按钮把设计令牌翻译成大白话；设计库提供统一术语模板，降低设计词汇门槛。
- **开发者/AI Agent 友好**：MCP 工具与 Dashboard 走同一条 `design-service`/`stateStore` 代码路径，不会双轨漂移。

---

## 3. 目标用户与画像

| 类型 | 画像 | 典型诉求 |
|---|---|---|
| **主要用户：非设计专业用户** | 创业者、独立开发者、产品经理、运营、AI 创作者 | 已有自己或 AI 生成的网站/App，想调整 UI/UX 但不熟悉设计词汇与代码 |
| **次要用户：开发者/AI Agent** | 使用 Trae、Claude 等 Agent 的开发者 | 通过 MCP 工具批量生成、调整、审计 UI |
| **桌面用户** | 不想安装 Node/跑命令行的普通用户 | 双击打开桌面应用即用 |

---

## 4. 痛点分析

来源：`docs/archive/prism-nonprofessional-painpoints.md`。

| 编号 | 痛点 | 现象 | Prism 对应方案 |
|---|---|---|---|
| P1 | 空白页瘫痪 | 不会说设计词汇，面对空画布不知道第一句说什么 | 示例指令、快捷 chips、设计库模板入口、解读按钮教词汇 |
| P2 | 指令死胡同 | 非专业说法系统听不懂，排队后无反馈 | 指令引擎 v2 覆盖日常说法；未匹配时 REST/WS 双通道即时回执并给可点击建议 |
| P3 | 改不到点上 | AI 生成 80% 满意，剩下 20% 不会改 | 自然语言直达令牌；每个元素可直接编辑；设计库拖拽快速换装 |
| P4 | 破坏恐惧 | 怕试错后回不去 | 50 步撤销/重做、自动备份、版本快照、模板非破坏应用、回滚 |
| P5 | 结果读不懂 | 拿到 HTML 不知道怎么看、怎么交付 | 播放模式预览、新标签页预览、产物路径弹窗、CSS 引入指引 |
| P6 | 质量没底 | 不知道自己的设计"行不行" | 对比度检查口语化、`/api/explain` 大白话质量解读、a11y 审计 |

---

## 5. 产品目标与成功标准

### 5.1 北极星指标

**"导入自己的产品 → 流式预览中调整或套用设计库模板 → 一键应用"全程流畅、可回滚。**

### 5.2 成功标准

1. **导入成功率**：4 类来源（项目文件夹 / 网页 URL / HTML 代码或文件上传 / 实际界面截图）均可完成导入并自动拆分为可编辑元素。
2. **编辑体验**：
   - 每个元素（顶层组件与组件内部元素）均支持：拖拽移动、8 向缩放、X/Y/W/H 精确数值、多选、对齐/分布、Z 轴排序、图层重命名。
   - 标尺/参考线/吸附可用，吸附阈值 5px。
   - 组件内部元素可编辑文本、属性与 CSS；可独立设置为组件（类型/属性/外观），保留原位渲染。
   - 组件可任意组合：可拖拽加入其他组件成为子组件，支持嵌套、移出与重排序。
3. **设计库快速变更**：24 个设计风格一键换装；117 个组件模板及其变体支持拖拽添加、拖入其他组件、就地替换；全部可撤销。
4. **一键应用**：
   - 从原始文档重建产物，脚本与 `<head>` 不丢失。
   - 纯 HTML 文件来源原位写回，`.prism-backups` 备份可回滚。
   - 应用结果弹窗展示产物路径与 CSS 引入指引。
5. **非破坏性**：所有变更走撤销/重做、版本、备份；模板应用不要求清空页面。
6. **质量底线**：仓库回归测试全绿（当前历史基线为 450 单测 + 12 e2e，功能裁剪后基线随代码同步调整），每改动必有测试。

---

## 6. 产品范围

### 6.1 范围内

- Web 可视化控制台：**流式预览视图 + 播放视图** + 属性面板 + 图层面板 + **设计库** + 令牌面板 + 版本/评论/活动日志。
- **设计库**：24 个设计风格、117 个组件模板及其变体；支持拖拽到预览添加、拖入其他组件形成组合、选中就地替换；内置术语模板。
- **组件组合能力**：组件可任意嵌套组合；内部元素可独立组件化（设置组件类型/属性/外观）。
- MCP 工具集（设计生成、精确编辑、审计、导入、一键应用、项目、版本等）。
- 内置 LLM 通道（BYO API Key：OpenAI 兼容 / Anthropic / Gemini）。
- Electron 桌面应用。
- 导入 → 调整 → 一键应用 → 回滚 完整管线（4 类来源）。
- 设计令牌系统。

### 6.2 范围外（v1.1 起明确不承诺/已移除）

- 自由画布与自由绘制（tldraw 绘制、形状/图片挂行为、画布双向映射）。
- 独立的组件库、独立的设计系统库（由统一设计库承接）。
- 交互模板（6 个一键绑定行为模板）。
- 首次原型的 AI 生成质量竞赛（AI 仅作脚手架）。
- 分享链接 / 模板市场。
- JSX/Vue 逐文件级别的写回（当前仅纯 HTML 文件原位写回）。
- 多人实时协作（当前为本地/单机状态，预留项目分桶与评论/活动日志）。

---

## 7. 核心概念与术语

| 术语 | 定义 |
|---|---|
| ComponentNode | 页面上的一个组件/元素节点：`id/type/variant/name/props/layout/visible/locked/children/animation` |
| PageDef | 页面：`id/name/components`；项目由多页组成，当前页为编辑目标 |
| ImportRecord | 导入来源记录：`url / html / file / capture` 四类，保存原始 HTML 快照、源 URL/文件路径 |
| 流式预览 | 单一智能画布视图：默认按真实高度自动纵向流式排布（不重叠），同时每个元素可自由拖动/缩放；所有元素在此视图中可被选中并编辑 |
| 播放模式 | 无编辑态的预览视图，用于查看与体验页面最终效果 |
| 设计库 | 统一的内容面板：聚合 24 个设计风格与 117 个组件模板及其变体，支持拖拽添加与就地替换 |
| 设计风格 | 设计库中的一键换装风格（24 个），应用到项目时覆盖设计令牌，可撤销 |
| 组件模板及变体 | 设计库中可拖拽的 117 个组件、布局及其变体，支持添加到页面、拖入其他组件或替换选中元素 |
| 内部元素组件化 | 选中组件内部元素后，可将其独立设置为组件（类型/变体/属性/外观），并保留原位渲染 |
| 组件任意组合 | 组件可拖拽加入其他组件成为子组件，支持任意层级嵌套、移出与重排序 |
| 术语模板 | 设计库条目的命名/描述/标签规范，用于统一设计库术语，帮助非专业用户检索和理解 |

---

## 8. 系统架构

### 8.1 总体架构

```
┌────────────────────────── Electron 桌面壳 ──────────────────────────┐
│  BrowserWindow（加载本地 Dashboard，client/）                        │
└──────────────┬──────────────────────────────────────────────────────┘
               │ http://127.0.0.1:<port>
┌──────────────▼──────────────────────────────────────────────────────┐
│  Node 服务（src/index.ts，随应用/命令行启动）                          │
│  ├─ MCP 层：stdio / Streamable HTTP                                │
│  ├─ LLM 通道（src/llm/）：OpenAI 兼容 / Anthropic / Gemini           │
│  │    └─ 系统提示词绑定组件 schema/模板 → 输出结构化 JSON             │
│  ├─ design-service / stateStore（唯一状态与变更路径）                  │
│  ├─ 设计库服务：风格/组件模板及变体/术语模板                          │
│  └─ REST + WebSocket 广播（客户端实时更新）                           │
└──────────────────────────────────────────────────────────────────────┘
```

### 8.2 关键设计原则

1. **一条代码路径**：内置 LLM 与 MCP 工具都调用 `design-service`/`stateStore`，不会双轨漂移。
2. **Key 管理**：LLM API Key 仅存本地配置文件（`~/.prism/projects/llm-config.json` 或 `PRISM_PROJECT_DIR`），不上传、不入库、API 返回掩码。
3. **可回滚**：LLM 的每次生成走现有 undo/版本机制，生成不满意一键撤销。
4. **Electron 选型理由**：服务端已是纯 Node，主进程 fork 子进程即可；Tauri 需要 Rust 侧车，成本更高。
5. **项目分桶**：`getProjectStore(projectId)` 提供按项目隔离的状态与撤销历史，保留单例 `stateStore` 兼容旧流程。

### 8.3 技术栈

- TypeScript 5.7、Node ≥ 18（`package.json` engines；README 徽标显示 Node ≥ 20，推荐 20+）
- `@modelcontextprotocol/sdk` 1.6、Express 4、ws 8、zod 3
- 前端：原生 JS + tldraw 5（`client/src/canvas-entry.jsx` 打包为 `client/vendor/prism-canvas.js`，v1.1 起前端画布功能随产品裁剪调整）
- Electron 43
- 测试：Node 内置 test runner + Playwright 1.62

---

## 9. 功能需求（FR）

> 优先级说明：P0 = 已实现核心，P1 = 已实现补全，P2 = 规划中。

### 9.1 FR-1 流式预览与全元素编辑（P0）

**需求描述**：默认视图为流式预览（单一智能画布）：组件按真实高度自动纵向流式排布、不重叠，同时每个元素（顶层组件与组件内部元素）都具备完整的编辑能力。

**功能点**：

- 自动流式排布：组件按真实高度纵向排列、不重叠；缺失布局自动补全；用户拖动/缩放后位置被记录为布局坐标。
- 任意顶层组件与内部元素均可选中、拖拽移动、8 向缩放。
- 支持 X/Y/W/H 精确数值输入与缩放预设。
- 多选（Shift 累加/框选）后支持 8 种对齐/分布（左/水平居中/右/顶/垂直居中/底/水平分布/垂直分布）。
- Z 轴操作：置顶、置底、上移一层、下移一层；图层面板拖拽排序。
- 标尺沿视图边缘显示，刻度跟随缩放/滚动；从标尺拖出参考线，可移动、拖回标尺或双击删除。
- 吸附：拖拽/缩放时吸附参考线、视图中心/边缘、其他元素边缘/中心（5px 阈值 + 紫色吸附指示线）。
- 图层重命名：图层面板双击行内重命名，持久化到 `ComponentNode.name`，可撤销。
- 组件内部元素可编辑：双击文本进入行内编辑，支持数组路径（如 `items[0].title`）；可编辑文本、属性与 15 项 CSS。
- **内部元素组件化**：选中组件内部元素后，可独立设置组件功能（类型/变体/属性/外观），并保留原位渲染；组件化后的内部元素拥有独立组件节点，可继续参与组合。
- **组件任意组合**：组件可拖拽加入其他组件成为子组件；支持任意层级嵌套、移出父级、调整顺序；拖拽时命中父组件高亮放置区。
- 所有变更走撤销/重做（50 步）。

**验收标准**：

- 选中多个元素执行对齐/分布后，坐标符合所选模式。
- 标尺/参考线仅会话级存在，不进入导出产物。
- 图层重命名后刷新/重载状态保留，撤销可恢复。
- 内部元素可被设置为指定组件类型并保留原位渲染。
- 组件拖入其他组件后成为其子组件，图层树正确展示嵌套关系，可撤销。

### 9.2 FR-2 设计库（P0）

**需求描述**：统一设计库聚合 24 个设计风格与 117 个组件模板及其变体，支持拖拽添加、就地替换与一键换装；内置术语模板统一命名与描述。

**功能点**：

- 设计库为唯一内容面板（不再分设组件库与设计系统库）。
- **24 个设计风格**：一键换装，应用到当前项目，覆盖设计令牌，可撤销。
- **117 个组件模板及其变体**：
  - 拖拽到流式预览空白处 = 添加到当前页面。
  - 拖拽到已有组件内部 = 添加为该组件的子组件（组件任意组合）。
  - 「替换选中」开关开启时，点击模板 = 就地替换当前选中元素（同 id 同布局位置，可撤销）。
- **术语模板**：设计库条目的命名/描述/标签遵循统一模板，保证非专业用户可检索、可理解。
- 全通道：REST/WS/MCP 均可读取设计库目录并应用风格/组件模板。

**验收标准**：

- 应用设计风格后，全项目 token 变更，预览与导出即时生效，可撤销。
- 替换选中元素后 id 与布局不变，type/variant/props 更新，可撤销。
- 拖拽设计库组件到预览，生成新元素并自动保存。
- 拖拽设计库组件到已有组件内部，生成子组件并正确嵌套。

### 9.3 FR-3 播放模式（P0）

**需求描述**：提供无编辑态的播放视图，用于查看与体验页面最终效果。

**功能点**：

- 播放/编辑视图切换（播放时隐藏编辑操作与选中态）。
- 播放模式可完整预览导入页面的原始还原效果（配合原页面沙箱 iframe）。
- 播放模式可触发导入页面自带的原始交互（如页面内跳转、表单提交、脚本行为等），由原始文档在沙箱 iframe 中运行还原，不依赖 Prism 重新绑定行为。
- 从播放模式可返回流式预览继续编辑。

**验收标准**：

- 播放模式隐藏编辑控件与选中态，页面正常渲染。
- 导入页面的原始脚本与交互在播放模式可正常触发。
- 编辑/播放可来回切换，状态不丢失。

### 9.4 FR-4 导入 → 调整 → 一键应用管线（P0，差异化核心）

**需求描述**：用户导入自己的产品，在 Prism 中调整，然后一键写回真实产物，全程可回溯。

**功能点**：

- **导入向导**（4 类来源）：
  - 项目文件夹：扫描项目并逐页记录 provenance（`kind: file`）。
  - 网页 URL：抓取并拆成可编辑组件，保留完整原始文档。
  - HTML 代码 / HTML 文件上传：解析并拆组件。
  - 实际界面截图：以参考图方式导入（`/api/capture-client`）。
- **原页面 / 编辑切换**：URL/HTML/文件夹来源提供「◎ 原页面 / ✎ 编辑」切换；原页面用沙箱 iframe 完整还原 CSS/脚本/交互，编辑态为语义片段。
- **一键应用**（`POST /api/apply` → `src/apply.ts`）：
  - 无来源：导出 `prism-adjusted-<页面>.html` + `prism-adjustments.css`。
  - URL/HTML 来源：从原始文档重建产物，脚本与 `<head>` 不丢失；注入 `<base href>`（URL 来源）与 `prism-adjustments.css`。
  - 纯 HTML 文件来源：原位写回原文件，旁边生成 `.prism-backups` 备份；CSS 写到同目录。
- **备份与回滚**：覆盖前自动时间戳备份；`POST /api/apply/rollback` 恢复最近备份。
- **应用结果弹窗**：展示产物完整路径 + `<link rel="stylesheet" href="prism-adjustments.css">` 引入指引 + 就地回滚按钮。
- **导入横幅**：显示"已导入：来源（N 个组件）· 调整后点击一键应用"；所有来源统一记录 provenance。

**验收标准**：

- 导入 URL 后，编辑片段并应用，产物保留原始 head 与脚本。
- 纯 HTML 文件应用后原文件被修改，同时 `.prism-backups` 产生备份，回滚后内容恢复。
- 导入记录可在 `GET /api/imports` 查询。

### 9.5 FR-5 内置 AI 与指令引擎（P0）

**需求描述**：控制台内置 LLM 通道，用户填自己的 API Key 即可自然语言生成/调整；本地指令引擎优先处理，未匹配自动回退。

**功能点**：

- AI 设置弹窗：服务商（OpenAI 兼容 / Anthropic / Gemini）、API 地址、Key、模型 + 测试连接；Key 掩码显示。
- 内置 LLM 生成结构化组件 JSON，走 `design-service`/`stateStore`，可撤销、可广播。
- 指令引擎 v2：覆盖改文字、相对字号/间距/圆角缩放、明暗调整、字体切换、文字颜色、对比度检查、重做、清空、模板/组件/设计风格等日常说法。
- 未匹配指令自动回退：REST/WS 双通道交给内置 AI，客户端显示"AI 正在生成页面…"→"已执行：AI 已生成…"。
- 「解读」按钮（`/api/explain`）：把令牌翻译成大白话（风格、主色、性格形容词、字体、对比度告警），并给 7 条可点击后续指令。
- 空状态示例指令，防止空白页瘫痪。

**验收标准**：

- 未配置 LLM 时，未匹配指令返回可点击建议。
- 配置 LLM 后，自然语言生成页面成功且可撤销。
- API Key 不通过 API 返回（仅掩码）。

### 9.6 FR-6 MCP 工具集（P0）

**需求描述**：通过 MCP 暴露设计生成、精确编辑、审计、导入、一键应用、项目、版本等能力，供 AI Agent 调用。

**功能点**：

- MCP 工具覆盖：
  - 基础生成：调色板、字体配对、字号阶梯、间距系统、阴影系统、圆角系统、渐变、断点、完整设计令牌。
  - 精确编辑：增删改组件、对齐/分布、Z 轴、重命名、动画、设计库应用。
  - 质量审计：对比度检查、a11y 审计、设计评审、语义风格、解读。
  - 导入与写回：网页导入、项目扫描、一键应用、回滚。
  - 项目与版本：项目保存/加载/版本/评论/平台快照。
- 支持 stdio 与 Streamable HTTP 双传输。
- 同时暴露 MCP resources 与 prompts。
- 工具变更与 Dashboard 走同一服务层，广播实时同步。
- **注意**：已删除功能（自由画布、交互模板等）对应的 MCP 工具需随代码裁剪同步下架，最终工具数以代码清单为准。

**验收标准**：

- `npm start` 通过 stdio 可被 MCP 客户端连接并列出工具清单。
- `TRANSPORT=http PORT=3100 npm start` 可提供 Streamable HTTP。

### 9.7 FR-7 项目管理、版本与协作（P1）

**功能点**：

- 项目保存/加载/自动保存（`project-store.ts`，`~/.prism/projects` 或 `PRISM_PROJECT_DIR`）。
- 多页面管理：新增/切换/删除/重命名页面。
- 版本快照：创建、列表、差异对比、恢复。
- 平台快照：按平台保存/恢复设计状态。
- 评论：对组件添加评论（上限 200 条）。
- 活动日志：记录最近 100 条操作，来源区分 AI/user。
- 项目分桶：`getProjectStore(projectId)` 支持多项目隔离编辑。

**验收标准**：

- 保存项目后重启服务可恢复页面、组件与令牌。
- 版本创建后修改设计，可恢复到指定版本。

### 9.8 FR-8 导出与交付（P0 基础 / P2 规划）

**功能点**：

- 导出多格式（HTML/CSS/React/Vue 等，实际以导出弹窗支持列表为准）。
- 新标签页打开 HTML 独立预览，可保存为单文件。
- 截图（PNG，HTML 降级）。
- 一键应用产物路径弹窗与 CSS 引入指引。
- **P2 规划**：分享链接、单文件导出增强、模板市场。

### 9.9 FR-9 Electron 桌面应用（P0）

**功能点**：

- `npm run app` 启动；主进程自动选择空闲端口、fork `server-host.cjs` 子进程（`ELECTRON_RUN_AS_NODE`）、健康检查通过后打开 BrowserWindow 加载本地 Dashboard。
- 单实例锁。
- 零 Node/命令行门槛，双击即用。

**验收标准**：

- Electron 启动后自动打开控制台，服务健康检查通过。
- 关闭应用后子进程服务退出。

---

## 10. 非功能需求（NFR）

| 类别 | 要求 |
|---|---|
| 安全 | LLM Key 仅本地存储，不上传、不入库、日志不记录、API 返回掩码；敏感信息不出本机 |
| 可靠性 | 所有写操作走备份/撤销/版本；导入源保留原始文档；应用/回滚幂等 |
| 性能 | Dashboard 通过 WS 广播实时同步；预览自动保存防抖；单页内数百元素可流畅编辑 |
| 可访问性 | WCAG AA 对比度检查；可见焦点态；`prefers-reduced-motion` 尊重；点击目标尺寸达标 |
| 兼容性 | Node ≥ 18（推荐 20+）；Chrome 110+；响应式 375/768/1024/1440 |
| 可维护性 | TypeScript 严格构建；ESLint + Prettier；模块化 routes/tools/service/state |
| 可测试性 | 仓库回归测试全绿；每改动必有测试；`npm run check` 通过 |

---

## 11. 数据模型摘要

见 `src/state.ts` 与 `src/types.ts` 完整定义，核心结构如下：

```ts
interface DesignToken {
  value: string;
  source: "ai" | "user" | "preset";
  description?: string;
}

interface DesignTokens {
  colors: Record<string, DesignToken>;
  typography: Record<string, DesignToken>;
  spacing: Record<string, DesignToken>;
  shadows: Record<string, DesignToken>;
  radii: Record<string, DesignToken>;
  transitions: Record<string, DesignToken>;
}

interface ComponentNode {
  id: string;
  type: string;
  variant?: string;
  name?: string;
  props: Record<string, unknown>;
  layout?: { x: number; y: number; w: number; h: number };
  visible?: boolean;
  locked?: boolean;
  children: ComponentNode[]; // 任意层级嵌套组合
  animation?: AnimationDef;
}

interface ImportRecord {
  kind: "url" | "html" | "file" | "capture";
  source: string;
  url?: string;
  html_file: string;
  imported_at: string;
  component_count: number;
  base_url?: string;
  source_file?: string;
  source_is_html?: boolean;
}
```

---

## 12. 接口摘要

### 12.1 MCP

- 前缀多为 `design_*`（精确编辑）与 `ui_*`（基础生成）。
- 传输：stdio（默认）与 Streamable HTTP（`TRANSPORT=http`）。
- 已删除功能对应工具需随代码裁剪同步下架。

### 12.2 REST（部分核心）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/import/product` | 导入项目文件夹 / URL / HTML |
| POST | `/api/capture-client` | 导入实际界面截图 |
| GET | `/api/imports` | 查询导入记录 |
| POST | `/api/apply` | 一键应用（生成/写回产物） |
| POST | `/api/apply/rollback` | 回滚最近备份 |
| POST | `/api/prompt` | 提交自然语言指令 |
| GET | `/api/explain` | 设计解读 |
| GET | `/api/design-library` | 设计库目录（设计风格/组件模板及变体/术语模板） |
| POST | `/api/design-library/style` | 应用设计风格 |
| POST | `/api/design-library/component` | 添加或就地替换组件模板 |
| PUT | `/api/component/:id/replace` | 就地替换组件 |
| PUT | `/api/component/:id/name` | 组件重命名 |

### 12.3 WebSocket

- `prompt` / `prompt_result`：指令提交与执行回执。
- `apply_component_template` / `rename_component` 等：客户端编辑消息。
- `change` 类广播：状态变更实时同步到所有连接的客户端。

---

## 13. 测试与质量基线

- 单元/集成测试：以仓库当前回归基线为准（历史口径 450 项，`npm test`）。
- e2e：12 项（Playwright，`tests/e2e/dashboard.spec.ts`），覆盖 Dashboard 主流程、导入→应用→回滚等；功能裁剪后需同步调整用例。
- 静态检查：`npm run lint`、`npm run build`（tsc）。
- 回归原则：每改动必有测试；`npm run check` 作为合入门槛。

---

## 14. 里程碑与当前状态

| 优先级 | 里程碑 | 状态 |
|---|---|---|
| P0 | 开箱即用（Electron + 内置 LLM 通道） | ✅ 已完成 |
| P0 | 流式预览与全元素编辑 | ✅ 已完成（v1.1 口径确认） |
| P0 | 自己的产品管线（四来源导入 + 一键应用 + 回滚 + 原始文档重建） | ✅ 已完成 |
| P0 | 设计库（24 设计风格 + 117 组件模板及变体 + 拖拽/替换） | ✅ 已完成（v1.1 口径确认） |
| P0 | 播放模式 | ✅ 已完成 |
| P1 | 设计库术语模板完善 | 🟡 进行中/下一步 |
| P1 | 已删除功能的代码与测试裁剪同步 | 🟡 待执行 |
| P2 | 交付闭环（分享链接、单文件导出、模板市场） | ⬜ 规划中 |

---

## 15. 风险与开放问题

| 风险/问题 | 说明 | 应对 |
|---|---|---|
| 内置 AI 生成质量 | 不再追加生成质量投入，AI 仅脚手架 | 明确定位为"调整"而非"生成"；本地指令引擎兜底 |
| 导入还原复杂度 | URL/HTML 页面结构千差万别，语义片段拆分可能不准 | 保留原始文档沙箱还原；片段编辑 + 全文档重建；提供回滚 |
| 项目文件夹来源的框架文件写回 | JSX/Vue 逐文件映射尚未完成 | 当前纯 HTML 原位写回；JSX/Vue 走生成产物路径，列为 P1 增强 |
| 功能裁剪后的代码同步 | 自由画布、交互模板、组件库/设计系统库相关代码与 MCP 工具仍需裁剪 | 按 PRD v1.1 逐项下架并同步测试 |
| 桌面应用分发 | Electron 二进制需在目标机安装时下载 | 已提供 `npm run app`；后续可做 `electron-builder` 打包分发 |
| 多用户协作 | 当前仅本地单机状态 | 已预留项目分桶与评论/活动日志；真正多人实时协作未承诺 |

---

## 16. 已确认决策

1. **流式预览 = 单一智能画布**：默认按真实高度自动纵向流式排布（不重叠），同时每个元素均可自由拖动/缩放；不再有流式/自由二元切换。
2. **设计库接口**：统一使用 `/api/design-library` 系列接口（目录 / 风格 / 组件），旧模板目录接口由设计库接口承接。
3. **MCP 工具口径**：以 `design_list_capabilities` 为唯一清单；画布绘制与交互模板相关工具随功能裁剪下架，其余保留。

---

## 17. 附录

- 完整设计方案：`DESIGN.md`
- 产品定义 v3.2：`prism-product-definition-v2.md`
- 非专业用户痛点分析：`docs/archive/prism-nonprofessional-painpoints.md`
- UI/UX 设计规范：`docs/archive/prism-uiux-design-plan.md`
- 核心源码：
  - 状态模型：`src/state.ts`
  - 设计服务：`src/service/design-service.ts`
  - 组件 schema：`src/component-schemas.ts`
  - 设计库/模板目录：`src/template-catalog.ts`、`src/style-guides.ts`
  - 一键应用：`src/apply.ts`
  - 导入解析：`src/import-project.ts`
  - 写回：`src/writeback.ts`
  - LLM 通道：`src/llm/`
  - 路由：`src/routes/`
  - 工具：`src/tools/`
  - 客户端：`client/`
  - 桌面壳：`electron/`