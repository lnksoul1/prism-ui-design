# Prism 产品定义 v3.2（讨论收敛版 · 定位第三次修正）

> 版本：v3.2 · 日期：2026-08
> 依据：产品形态讨论 + 三次定位修正：
> 1. "目标不是一次性生成顶尖 UI，而是让用户能对自己产品的 UI 进行精确、自由的调整"
> 2. "**Prism 不负责首次生成原型**——用户导入自己做的网站/App，通过 Prism 调整，最好一键应用"
> 3. "产品定位是非设计专业用户，可通过本产品对其**自己创作或 AI 创作的网站/App** 的 UI/UX 进行**精确、自由地调整**；对于**任何一个组件、按钮、交互**都可以任意调整；用户**随意绘制图形或插入图片并赋予相应的功能**；可通过本产品**预设风格、组件、交互模板快速变更，也可以通过自己微调**"
> 关联：[`prism-nonprofessional-painpoints.md`](./prism-nonprofessional-painpoints.md)、[`prism-uiux-design-plan.md`](./prism-uiux-design-plan.md)

---

## 0. 一句话定位（v3.2）

**Prism 是非设计专业用户的可视化调整台：导入自己创作或 AI 创作的网站/App → 在画布上精确、自由地调整任何一个组件、按钮、交互（数值、参考线、对齐、图层、行为、绘制、图片）→ 一键应用回真实产物。想快，用预设的风格/组件/交互模板快速变更；想细，自己微调。AI 生成只是可选的脚手架。**

## 1. 四项拍板（产品形态）

| 维度 | 决定 | 含义 |
|---|---|---|
| 目标用户 | **非设计专业用户为主** | 用户已有自己的产品（**自己创作或 AI 创作的网站/App**）；Prism 负责**调整**而非首造原型 |
| 载体 | **打包桌面应用** | 双击即用，零 Node/命令行门槛（Electron 壳 + 内置 Node 服务） |
| AI 来源 | **内置 LLM 通道，BYO API Key** | 辅助批量调整/建议；不做首造生成的主通道（"AI 创作"指产品的来源可以是外部 AI，Prism 对这类产品同样负责调整） |
| 成功标准 | **"导入自己的产品 → 精确自由调整或模板快速变更 → 一键应用"全程流畅、可回滚** | 编辑体验为北极星：精确（数值/参考线/吸附）· 自由（任意操作）· 快速（预设模板）· 一键应用（写回真实产物）· 永不破坏（撤销/版本） |

## 2. 用户使用流（v3.2 修正）

```
导入自己的产品（自己创作或 AI 创作的网站/App：网页 URL / HTML / 已有代码）→ 自动拆成可编辑组件
→ 精确调整：输入精确数值、参考线吸附、对齐分布、Z 轴、图层
→ 自由调整：拖动/缩放/多选、任意绘制图形、插入图片并赋予功能
→ 关联功能：组件/图形/图片绑定交互（跳转、链接、显隐、提示…）
→ 快速变更：套用预设风格 / 组件 / 交互模板一键换装；或自己微调（两者可混用）
→ 一键应用：调整写回真实产物（CSS/HTML/代码），可预览、可回滚
（可选）用 AI/模板生成一个起点脚手架，再进入上述调整流
```

**核心心智**：导入 = 开始；调整（或套模板）= 核心价值；一键应用 = 闭环。

## 3. 能力支柱与现状差距（v3 重排）

| 支柱 | 现状（已实现） | 差距 | 优先级 |
|---|---|---|---|
| **① 精确编辑（新核心）** | ✅ 自由模式 X/Y/W/H 输入、外观面板、缩放预设、多选（Shift）+ 8 种对齐/分布、Z 轴置顶/置底/上下移、图层拖拽排序、**标尺/参考线/吸附**、**图层重命名** | 🟡 数值微调面板可再增强（渐变/圆角可视化） | **P0 ✅（已完成）** |
| **② 自由编辑** | freeform 拖拽/8 向缩放、tldraw 绘制（矩形/箭头/文字/便签/图片）、绘制工具竖栏 | 🟡 图片拖入画布并赋行为 | P1 |
| **③ 功能关联（交互）** | ✅ **通用行为模型**（navigate/link/toggle/toast/submit/prompt）+ 检查器交互编辑器 + 播放模式触发 | 绘制形状/图片挂行为 | P1（补缺口） |
| **④ 自己的产品管线（差异化）** | ✅ 导入向导（项目文件夹/网页 URL/HTML 上传/客户端界面/实际界面截图）→ 拆组件 → 一键应用（HTML+CSS 写回，自动备份）→ 回滚；全部来源记录 provenance，横幅/应用/回滚一等旅程闭环；应用结果弹窗展示产物路径与 CSS 引入指引 | 🟡 项目文件夹来源的页级精细映射（JSX/Vue 逐文件）可再增强 | **P0（与①并行）** |
| **⑤ AI 生成（脚手架）** | ✅ 内置 LLM 通道（OpenAI 兼容/Anthropic/Gemini）+ 指令引擎 + 模板 | 不再追加生成质量投入，只保底 | 维持 |
| **⑥ 即时交付** | 导出多格式 + 新标签页打开 + Electron 壳 | 分享链接/模板市场 | P2 |
| **⑦ 预设模板快速变更（v3.2 新增支柱）** | ✅ 14 风格预设（含 8 趋势风格）+ 每风格动效配置 + 5 页面模板 + 41 组件类型（库拖拽上画布）+ 风格预设升级方案（14→30） | ❌ **组件模板**（可直接替换现有组件的成品块）、**交互模板**（行为组合一键套用）、**"套用模板到当前产品"的快速变更旅程**（目前模板是开新页面，而非换装当前产品） | **P0（与①并行）** |

## 4. 架构设计（目标态）

```
┌────────────────────────── Electron 桌面壳 ──────────────────────────┐
│  BrowserWindow（加载本地 Dashboard，现有 client/ 零改动）            │
└──────────────┬──────────────────────────────────────────────────────┘
               │ http://127.0.0.1:<port>
┌──────────────▼──────────────────────────────────────────────────────┐
│  Node 服务（现有 dist/index.js，随应用启动）                          │
│  ├─ 现有 MCP 层（保留，高级扩展）                                    │
│  ├─ 【新增】LLM 通道：OpenAI 兼容 chat completions                    │
│  │    └─ 系统提示词绑定组件 schema/模板 → 输出结构化 JSON             │
│  │    └─ 复用现有 service/stateStore（与 MCP 工具同一代码路径）       │
│  ├─ 【新增】行为模型：ComponentNode.behavior { type, params }         │
│  │    └─ 渲染层绑定点击 → 跳转/链接/显隐/提示/表单                    │
│  └─ 现有 REST/WS 广播（客户端实时更新）                               │
└──────────────────────────────────────────────────────────────────────┘
```

要点：
- **一条代码路径**：内置 LLM 与 MCP 工具都调用 `design-service`/`stateStore`，不会双轨漂移。
- **Key 管理**：本地配置文件（`~/.prism/config.json`），不上传、不入库。
- **可回滚**：LLM 的每次生成走现有 undo/版本机制，生成不满意一键撤销。
- **Electron 选型理由**：服务端已是纯 Node，主进程 fork 子进程即可；Tauri 需要 Rust 侧车，成本更高。

## 5. 行为模型（支柱③的初步设计）

```ts
// 任何可点击对象（组件 / 绘制的形状 / 图片）都可以挂一个行为
type Behavior =
  | { type: "navigate"; page_id: string }        // 跳转页面（现有 play 链接泛化）
  | { type: "link"; url: string; new_tab?: boolean } // 打开链接
  | { type: "toggle"; target_component_id: string }  // 显示/隐藏目标组件
  | { type: "toast"; message: string }           // 提示反馈
  | { type: "submit"; form_id?: string }         // 表单提交（跳转/提示）
  | { type: "prompt"; prompt: string }           // 触发一条指令（AI 联动）
```

- 挂载位置：`ComponentNode.behavior`；绘制形状/图片用 tldraw 形状 `meta.behavior`。
- 触发时机：预览模式点击（现有 play 模式的泛化）。
- 检查器增加「交互」区：选择一个行为类型 + 参数（现有页面跳转选择器升级为通用行为编辑器）。

## 6. 路线图（v3 修正：以"精确自由调整"为北极星）

| 优先级 | 里程碑 | 内容 | 验证 |
|---|---|---|---|
| **P0 ✅** | 开箱即用 | Electron 壳 + 内置 LLM 通道（BYO Key）+ 一句话/模板生成入口 | 双击即用 |
| **P0 ✅** | 功能关联 | 通用行为模型（navigate/link/toggle/toast/submit/prompt）+ 检查器交互编辑器 + 播放模式触发 | 组件都可点出功能 |
| **P0 ✅** | **精确编辑（新核心）** | 多选（Shift/框选）+ 对齐/分布 + **标尺/参考线/吸附** + Z 轴置顶/置底 + 图层拖拽排序/重命名 + 所有数值可精确输入 | "想把元素放到哪里、改成多大、什么颜色，都能精确做到" |
| **P0 ✅** | **自己的产品管线（差异化）** | "导入（网页/HTML/客户端界面/截图/文件夹）→ 调整 → 写回真实产物"一等旅程：来源归一 + provenance + 横幅 + 一键应用 + 回滚 + 产物路径弹窗 | 调整真实产品的 UI 并一键生效 |
| **P0 🆕** | **模板快速变更（v3.2 新增）** | 预设风格/组件/交互模板一键套用到当前产品（非开新页面）：风格换装、组件块替换、行为组合套用；与手绘/微调并存，可撤销 | "想快就套模板，想细就自己微调" |
| **P1** | 自由编辑补缺 | 图片拖入画布并赋行为；绘制形状可挂行为 | 图片/图形也能交互 |
| **P2** | 交付闭环 | 分享链接、单文件导出、模板市场 | 做出来能发出去 |

### 实施状态（2026-08）

- ✅ **内置 LLM 通道**（`src/llm/`）：config 本地存取（Key 只在本机）、三厂商适配（OpenAI 兼容/Anthropic/Gemini）、生成代理（系统提示词绑定组件 schema → 结构化 JSON → 复用现有 service/stateStore 代码路径，可撤销可广播）。**定位：脚手架，不再追加生成质量投入。**
- ✅ **指令自动回退**：本地指令引擎未匹配且已配置 LLM 时，REST/WS 双通道自动交给内置 AI，客户端显示"AI 正在生成页面…"→"已执行：AI 已生成…"。
- ✅ **AI 设置弹窗**：服务商/API 地址/Key/模型 + 测试连接 + 已保存 Key 掩码显示；指令栏 AI 徽标。
- ✅ **Electron 壳**（`electron/main.cjs` + `server-host.cjs`）：自动端口 + ELECTRON_RUN_AS_NODE 子进程 + 健康检查 + 单实例；`npm run app` 启动（二进制需在目标机安装 electron 时下载）。
- ✅ **行为模型（P1→P0）**：`ComponentNode.behavior`（navigate/link/toggle/toast/submit/prompt）+ `design_set_behavior` MCP 工具 + WS `set_behavior` + `PUT /api/component/:id/behavior`；检查器「交互」编辑器（类型选择 + 按类型参数）；播放模式点击触发（含旧 pageLink 兼容）；`toggle` 可显隐任意目标组件、`prompt` 可直接联动内置 AI。
- ✅ 测试：行为模型 state/service/server/tools 全链路（**总数 412 → 416**）+ e2e 播放模式改为行为编辑器流程（6/6 保持）；浏览器实测 toast/toggle 触发正常、零报错。
- ✅ **精确编辑（P0）**：多选（Shift 累加）+ 8 种对齐/分布（`alignComponents`，单步撤销）+ Z 轴置顶/置底/上下移（`zOrderComponent`）+ 图层拖拽排序；全通道（WS/REST/MCP `design_align_components`/`design_z_order_component`）；e2e"多选+对齐"用例。
- ✅ **标尺 / 参考线 / 吸附（P0，精确编辑收尾）**：
  - 自由模式画布边缘标尺（刻度跟随缩放/滚动），从标尺拖出参考线、拖动移动、拖回标尺或双击删除（会话级编辑辅助，不进导出）。
  - 拖动/缩放组件时吸附到参考线、画布中心/边缘、其他组件边缘/中心（5px 阈值 + 紫色吸附指示线）。
  - **图层重命名**：`ComponentNode.name` + `state.renameComponent`（可撤销）+ WS `rename_component` + REST `PUT /api/component/:id/name` + MCP `design_rename_component`（工具 74 → 75）；图层面板双击名称行内重命名。
  - 测试：state/service/server/tools 全链路 + e2e"重命名 + 标尺参考线出现"（**总数 446 → 449，e2e 10 → 11**）。
- ✅ **导入 → 一键应用 管线（P0，差异化）**：
  - 导入向导（`… → 导入`）：项目文件夹 / 网页 URL / HTML 代码（含文件上传），`POST /api/import/product` 抓取并拆成可编辑组件；来源记录入状态（`imports`，页面级 provenance）。
  - 一键应用（`POST /api/apply` → `src/apply.ts`）：写 `prism-adjusted-<页面>.html`（所见即所得）+ `prism-adjustments.css`（令牌覆盖，链接到你的产品即生效）到 `PRISM_PRODUCT_DIR`（默认 `~/.prism/products`），覆盖前自动时间戳备份。
  - 回滚（`POST /api/apply/rollback`）：恢复最近备份。
  - 客户端：导入横幅（"已导入：来源（N 个组件）· 调整后点击一键应用"）+ ✍ 一键应用 / ↺ 回滚按钮。
  - 测试：apply 单元 4 项 + state 导入记录 + server 管线链路（导入→调整→应用→备份→回滚）+ e2e 浏览器全流程（**总数 423 → 429，e2e 7 → 8**）。
- ✅ **导入管线向导化收尾（P0，支柱④）**：
  - **来源归一**：导入向导新增「客户端界面」「实际界面截图」两个来源（`/api/import-client`、`/api/capture-client`）；文件夹导入逐页记录 provenance（`kind: file`）。
  - **旅程闭环**：所有来源统一走 `recordProductImport` → 导入横幅 → 一键应用 → 回滚（`ImportRecord.kind` 扩展 `client`/`capture`）。
  - **应用结果弹窗**：一键应用后展示产物完整路径 + `<link rel="stylesheet" href="prism-adjustments.css">` 引入指引 + 就地回滚按钮。
  - 测试：server 增加 import-client/capture provenance 断言（**总数 446 保持**）+ e2e 新增"客户端界面来源 → 横幅 → 应用 → 回滚"（**e2e 9 → 10**）。

- ✅ **模板资产（支柱⑦的已有基础）**：14 风格预设（`STYLE_PRESETS`）+ 每风格动效（`STYLE_MOTION_PROFILES`）+ 5 页面模板 + 41 组件类型（组件库拖拽上画布）；风格预设升级方案见 [`prism-style-preset-upgrade-plan.md`](./prism-style-preset-upgrade-plan.md)（14→30、完整设计系统字段）。
- ✅ **模板快速变更（P0，支柱⑦）**：  - 组件模板（`src/template-catalog.ts` `COMPONENT_TEMPLATES`，10 个成品块）：Hero 分屏+CTA / 导航+按钮 / 定价三档 / 注册表单（自带提交行为）/ 评价墙 / 统计条 / FAQ / CTA 横幅（自带链接行为）/ Cookie 横幅 / 便当盒网格。
  - **就地替换**（`state.replaceComponent`）：同 id 同布局位置换装，可撤销；组件库新增「替换选中」开关，点块即替换、拖到画布即添加。
  - 交互模板（`BEHAVIOR_TEMPLATES`，6 个）：打开链接（新标签页）/ 点击提示 / 跳转首页 / 显隐切换 / 表单提交反馈 / AI 联动指令；组件库新增「交互」tab，点选即绑定到选中组件（播放模式触发）。
  - 风格换装本就就地在当前产品上生效（`apply_style` / 风格库点击）。
  - 全通道：WS `apply_component_template`/`apply_behavior_template` + REST `GET /api/template-catalog`、`POST /api/templates/component`、`POST /api/templates/behavior`、`PUT /api/component/:id/replace` + MCP `design_apply_component_template`/`design_apply_behavior_template`（工具总数 72 → 74）。
  - 测试：state 替换 3 项 + service 模板 8 项 + server 全链路 + e2e"组件模板替换选中 + 交互模板绑定"（**总数 429 → 446，e2e 8 → 9**）。

## 7. 不变的东西

- 现有 416 单测 + 6 e2e 作为回归基线；每改动必有测试。
- 设计语言维持"现代极简毛玻璃"；UI/UX 方案（`prism-uiux-design-plan.md`）继续作为界面规范。
- MCP 层保留：对开发者用户仍然有价值，且内置 LLM 与它共享同一代码路径。

---

*本文档是产品讨论的收敛锚点。v3.2 第三次修正定位（"任何组件/按钮/交互都可调 + 绘制/插图赋功能 + 预设模板快速变更或自己微调"）后，三项 P0 已全部完成：精确编辑（多选/对齐/吸附/参考线/Z 轴/图层/重命名）+ 自己的产品管线（五来源归一 + 一键应用 + 回滚）+ 模板快速变更（组件/交互模板）。下一步按 P1 推进：自由编辑补缺（图片/形状挂行为）、风格预设升级（14→30）。*
