# Prism 针对性 UI/UX 设计方案

> 版本：v1.0 · 日期：2026-08
> 对象：`prism-ui-design-mcp` Dashboard（`client/`，原生 HTML/CSS/JS + tldraw 画布）
> 关联文档：[`prism-nonprofessional-painpoints.md`](./prism-nonprofessional-painpoints.md)（痛点分析）、[`prism-pixso-reference.md`](./prism-pixso-reference.md)（Pixso 对标）
> 性质：**面向非专业人员 + AI Agent 双主体的"AI UI 生成/评审"协作画布**，非通用矢量编辑器

---

## 0. 摘要

Prism 的定位不是第二个 Figma，而是**"非专业者用自然语言/模板/绘制把界面做出来，并交给 AI 实时打磨"的协作画布**。当前 Dashboard 已具备三栏骨架、属性检查器、令牌面板、画布双模式、评论/版本/播放等能力，但存在一条体验主线未被打通：

> 用户**不知道从哪开始 → 说了听不懂 → 听不懂没反馈 → 听懂改不到点 → 改完不敢动 → 做完拿不走**。

本方案围绕这条主线，给出**信息架构、分区界面、关键旅程、视觉系统、状态设计、无障碍与落地路线**的针对性设计，并逐条映射到现有代码与设计令牌，保证可直接实施。

设计目标（可度量）：
- 新用户 **≤ 30 秒** 生成第一个可见页面（当前依赖外部 Agent，需降到"打开即有"）。
- 指令发出后 **≤ 1 秒** 有明确回执（已执行 / 已排队 + 可点击建议）。
- 高频精修（改字/改色/复制/缩放/图层）**≤ 2 次点击** 完成。
- 全程**可撤销、可回滚、无破坏性操作**。

---

## 1. 设计原则（贯穿所有界面）

| 原则 | 含义 | 落地判据 |
|---|---|---|
| **P1 说话即操作** | 界面把"设计词汇"翻译成按钮/示例，用户不必懂术语 | 每个面板空状态都给出可点击示例指令 |
| **P2 即时反馈** | 任何动作 1 秒内有状态回执，绝不沉默 | 指令回执、保存/复制/应用都有 toast + 状态位 |
| **P3 所见即所得** | 预览即最终产物，画布=交付物 | 导出/预览/截图/独立页面四入口一致 |
| **P4 永不破坏** | 试错零成本：可撤销、非破坏模板、自动保存、版本 | 破坏性操作有二次确认或先快照 |
| **P5 渐进披露** | 高级能力收纳在次级入口，主界面只留高频 | 顶栏精简、检查器分组折叠、导出格式折叠 |
| **P6 双主体一致** | 用户与 AI 看到同一份状态、同一套语义 | 令牌/组件/评论对人和 AI 都是同一 schema |

---

## 2. 用户画像与核心场景

| 画像 | 特征 | 核心诉求 | 对应旅程（§6） |
|---|---|---|---|
| **非专业者 A**（产品/运营/创业者） | 不会设计词汇、不会代码 | 快速出一个能看、能改、能交付的页面 | J1 首次上手、J2 生成循环 |
| **非专业者 B**（已有参考图/灵感） | 有截图或手绘线框 | 把参考"落"成页面 | J2（画布绘制 → 转组件） |
| **AI Agent** | 通过 MCP 工具操作同一状态 | 需要可观测、可回滚、可自检 | J2（渲染回显 + 审计闭环） |
| **评审者 C**（老板/协作方） | 只提意见不操作 | 打点批注、看演示 | J3 评审与演示 |

---

## 3. 信息架构（布局蓝图）

维持 **三栏 + 画布 + 底部指令栏** 骨架（已与 Figma/Pixso 心智对齐），重点调整**优先级与分组**：

```
┌──────────────────────────────────────────────────────────────────────────┐
│ 顶栏：Logo·项目名 · 保存/版本指示 · ── 居中:全局搜索(Ctrl+K) ── 在线人数 · 分享 · 播放 · 导出 · 主题 · 语言 │
├────────────┬──────────────────────────────────────────────┬───────────────┤
│ 左 280px   │  画布工具栏                                   │ 右 300px      │
│ ─────────  │  预览|画布 · 平台切换 · 缩放(适应/100%) · 元信息 │ ────────────  │
│ 页面标签    │ ──────────────────────────────────────────── │ 检查器/变量   │
│ (+新建)    │                                                │ (Tab 切换)    │
│ ─────────  │   画布（点阵网格 · 设备框 · 实时光标）          │  属性 | 代码  │
│ 图层面板    │   选中→浮动操作条(复制/层级/删除/链接)          │  外观/布局/   │
│ 资产库      │                                                │  内容/动效/   │
│ (风格·动效  │                                                │  交互/操作    │
│  组件·模板  │                                                │              │
│  ·设计系统) │                                                │ 变量面板:     │
│ 版本·评论   │                                                │  色彩/字体/   │
│ 活动日志    │                                                │  间距/阴影/   │
│            │                                                │  圆角 + 冲突  │
├────────────┴──────────────────────────────────────────────┴───────────────┤
│ 指令栏：建议条(未听懂时) · 示例指令chips · 输入框 · 发送 · 回执状态 · 解读按钮 │
└──────────────────────────────────────────────────────────────────────────┘
```

与现状的关键差异（按优先级）：

| 变更 | 现状 | 目标 | 优先级 |
|---|---|---|---|
| 左栏把「资产库」提升为一级（含组件/风格/模板/设计系统） | 已是二级 Tab | 保持，但**加缩略图预览**与"拖到画布"更醒目 | P1 |
| 右侧改为「检查器 / 变量」两个顶层 Tab | 检查器 + 令牌上下堆叠 | 明确"对象属性 vs 全局变量"两个心智 | P1 |
| 顶栏新增「分享」与「播放」主入口 | 播放在画布栏、无分享 | 演示/分享是交付关键 | P1 |
| 全局搜索/命令面板常驻顶栏居中 | Ctrl+K 隐藏 | 新用户发现不了命令面板 | P1 |

---

## 4. 分区详细设计

### 4.1 顶栏

- **左侧**：Logo → 项目名（可点击重命名）→「已保存/有未保存改动」小圆点（复用 autosave 状态）。
- **居中**：全局搜索框（`Ctrl+K`，即现有命令面板入口），placeholder 区分场景："搜索命令，或直接输入指令…"。
- **右侧（精简为高频）**：在线人数、**分享**（导出分享弹窗）、**播放**（演示）、导出、主题、语言。其余（截图/项目/写回/导入/解读）收入"…"更多菜单或命令面板。
- **规则**：顶栏按钮 ≤ 8 个；超过则进"…"。

### 4.2 左侧栏（导航 + 结构 + 资产）

- **页面标签**：横向 chips + "＋"，支持重命名（双击）、删除（右键/悬浮 ×）。当前页高亮。
- **图层面板**：树状展示（复用现有 `renderLayerPanel`），每条含：显隐眼睛、锁定锁、名称、类型图标；顶部"自动排列"快捷；**删除/复制/上下移**由选中浮动条承担，图层面板只保留显隐/锁定/重命名。
- **资产库**（一级 Tab，现有 `libStyles/libAnimations/libComponents/libTemplates/libDesignSystems` 保留）：每项**带语义缩略图**（组件用现有渲染器，风格用色卡 chips，模板用 `tpl-thumb`），悬停放大预览，支持拖到画布/点击应用。搜索框过滤。
- **版本 / 评论**：保持二级 Tab；版本行加"并排对比"入口（后续）。
- **活动日志**：底部，含来源过滤（AI/用户）与关键词；新增"回放上一步"（悬停高亮对应变更）。

### 4.3 画布与选择

- **画布工具栏**（左→右）：预览|画布切换 → 平台切换 → 缩放（−/百分比/+/适应/100%）。
- **画布**：点阵网格背景 + 设备框（现有 `platform-chrome`）。画布双模式：
  - **预览模式**：流式/自由定位（现有 `layout-mode-btn`），组件可点选。
  - **画布模式**：tldraw 全功能 + 模板优先（现有 `canvas-template-modal`）。
- **选中状态**：选中组件出现 `selection-toolbar`（复制/上移/下移/删除 + 新增「跳转链接」快捷），组件本身高亮 + 8 向缩放手柄（自由模式）。
- **多选**：Shift 点选/框选，选中多个时工具栏显示"对齐/分布/成组"（后续 P2）。
- **实时协作**：远程光标带用户标签（现有），多人编辑同一组件时冲突提示（现有 `conflict` 横幅保留）。

### 4.4 右侧检查器（对象属性）

改为两层 Tab：**属性 | 代码**（现有），并新增/强化分区（自上而下，按 Pixso 顺序）：

1. **对象头**：类型/变体 + ID（现有）。
2. **外观**（新增，已实现）：填充、文字颜色、圆角。
3. **布局**：X/Y/W/H（自由模式）；流式模式显示为只读或"切换到自由模式以精确定位"提示。
4. **内容**：字符串 props（现有），数组 props（如 items/links）给"编辑子项"折叠列表（后续）。
5. **动效**：入场/悬停/时长（现有）。
6. **交互**：页面跳转选择器（现有 `playLink`）。
7. **操作**：复制 / 删除（现有）。

### 4.5 变量面板（全局令牌）

- 令牌分组 Tab（色彩/字体/间距/阴影/圆角，现有）保留；顶部**冲突警告**（现有 `conflict-warnings`）前置为醒目横幅。
- 每个令牌值显示 `来源徽标`（AI/用户/预设/语义，复用 token.source）与语义 reason（悬停 tooltip）。
- 顶部加「深色/浅色」快捷切换与「对比度检查」按钮（复用 `/api/conflicts` 与指令"检查对比度"）。

### 4.6 指令栏（用户→AI 的主入口，本项目灵魂）

```
[未听懂建议条：'没听懂，已排队给 AI。你也可以说：' (chips ×4) ×]      ← 仅未匹配时出现
[示例指令 chips：深色模式 | 玻璃拟态 | 字太小了 | 加定价表 | 撤销 …]  ← 可滚动
[✧ 输入指令…                                          (发送) (解读)]  [状态: 已执行·主色已改 ✓]
```

- **示例 chips**：初始态展示"能一键执行"的指令（现有 9 个），滚动到底部有"更多示例"（展开面板，按"改样式/改内容/布局/质量"分类）。
- **状态回执**（现有 `prompt-status` 增强）：四种态——`排队中(灰) → Agent 已接收(蓝) → 已执行(绿 ✓) → 未听懂(橙 + 建议条)`，与 WS/REST 双通道一致。
- **解读按钮**（现有 `explain-btn`）：把当前设计翻成大白话并给出"你可以这样说"，**默认常驻在指令栏右侧**（比顶栏更贴合"说"的场景）。
- **快捷键**：`/` 聚焦输入（现有）；`↑` 恢复上一条指令。

### 4.7 空状态与首次引导

现有空画布 6 入口 + 示例指令（已实现）保留，**新增一个"新手三步"引导卡**（首次/空项目时）：

1. 说一句话（示例指令）→ 2. 或选模板 → 3. 或拖组件/画线框；每步一个高亮按钮，点击即执行。
- 引导卡在产生第一个组件后自动消失，并记住"已看过"（localStorage）。

### 4.8 弹窗与命令面板

- **导出弹窗**（现有）：主推「HTML + 新标签页打开 + 复制代码」三个动作；React/Vue/Figma/Svelte 等格式折叠进「更多格式」二级区，避免选择过载。
- **命令面板**（现有 Ctrl+K）：输入指令的优先级高于命令；命令按"页面/主题/模板/导出/画布"分组。
- **分享弹窗**（新增）：一键复制"独立预览链接/单文件 HTML/PNG"，对齐非专业者交付。

---

## 5. 视觉设计系统（映射现有令牌）

沿用现有**紫罗兰品牌体系**（`--accent #7C3AED`），本方案只做**语义化收敛**，不改品牌：

| 层 | 令牌（现有） | 用途规范 |
|---|---|---|
| 主色 | `color-primary` | 主按钮、选中态、焦点环、活跃图标 |
| 强调 | `color-accent` | 链接、hover 高亮、toast 图标 |
| 背景 | `color-bg` / `color-surface` | 画布底 / 面板底；深色模式自动切换（`themeMode`） |
| 文本 | `color-text` / `color-text-muted` / `color-text-dim` | 三级文本层级，正文 ≥ 13px，辅助 ≥ 11px |
| 边框 | `color-border` | 分隔线、卡片描边 |
| 圆角 | `radius-sm/md/lg/full`（8/12/16/9999） | 输入框 sm、卡片 md、浮层 lg、chips full |
| 阴影 | `shadow-sm/md/lg` | 卡片/浮层/弹窗三级海拔；深色模式用 border 替代阴影 |
| 间距 | `space-*`（4px 基数） | 面板内 8/12/16，区块间 24 |
| 字号 | `text-xs/sm/base/lg/xl/2xl` | 辅助 11/标签 12/正文 13/面板标题 12 大写/画布标题按组件令牌 |
| 动效 | `transition-fast/normal/slow` | hover 150ms、浮层 250ms、弹窗 400ms |
| 字体 | `font-display`(标题)/`font-body`(正文)/`font-mono`(ID/代码) | 数字与 ID 用 mono |

**焦点态**（无障碍，现有 Vercel 式双层焦点环保留）：`outline: 2px var(--accent) + 2px offset ring`。

**六态微交互**（现有 spec §5.3）：默认/hover/active/focus/disabled/loading，全部控件一致。

---

## 6. 关键用户旅程

### J1 首次上手（非专业者，30 秒出页面）
1. 打开空画布 → 看到"新手三步"引导卡 + 示例指令。
2. 点「生成一个 SaaS 模板」→ **内置引擎**立即出组件树 + 预览（无需 Agent）。
3. 点「解读」→ 大白话 + "你可以这样说"。
4. 换主题/改主色（示例 chip）→ 全程有回执。
- **成功判据**：无外部 Agent 时也能完成 1–4。

### J2 AI 生成与自检循环（Agent 侧 + 用户侧同屏）
1. 用户输入需求 → 指令回执（已执行/已排队）。
2. Agent `design_generate_page` / `design_add_component` → 画布实时更新（WS）。
3. Agent `design_render_preview`（HTML/PNG）→ 自我校验视觉。
4. Agent `design_audit_accessibility` → 冲突/建议写回评论或活动日志。
5. 用户用选中浮动条/外观面板精修 → 令牌 source 记为 user。
- **成功判据**：人机改动同源、可审计、可撤销。

### J3 评审与交付
1. 评审者 C 在预览/播放模式翻页、点组件（页面跳转）。
2. C 用评论（`design_add_comment`）打点，用户/Agent 侧可见。
3. 用户点「分享」→ 复制独立预览/单文件 HTML/PNG。
4. 用户点「导出」→ HTML 新标签页打开 → 保存或分享。

---

## 7. 状态设计（每个区域都定义状态）

| 区域 | 加载 | 空 | 错误 | 冲突/异常 |
|---|---|---|---|---|
| 画布 | 骨架网格 shimmer | 引导卡 + 示例指令 | 画布加载失败→"从组件重新生成"（已实现） | 冲突横幅 + 重新加载 |
| 检查器 | 占位 | "点击画布上的组件"（现有） | 组件已被删除→提示 + 清除选中 | — |
| 资产库 | "加载中…" | 分类空文案 | 加载失败 + 重试 | — |
| 令牌 | — | "令牌将在初始化后出现"（现有） | — | 对比度冲突横幅（现有） |
| 指令栏 | 发送中态 | 占位 + 示例 chips | WS 断开→ REST 回退 + "(WS↓)" | 未听懂→建议条（已实现） |
| 版本/评论 | "加载中…" | 空文案（现有） | — | — |
| 弹窗 | — | — | 错误 toast | — |

---

## 8. 无障碍（WCAG 2.1/2.2）

- 所有交互元素有 `aria-label`/`title`（现有 i18n 键补 `title` 语义）。
- 对比度：正文 `color-text`/`color-bg` ≥ 4.5:1（现有 `design_audit_accessibility` 兜底）。
- 目标尺寸：可点击控件 ≥ 24×24（现有 target-size 审计规则）。
- 键盘：完整走查（现有 `?` 帮助 / `Ctrl+K` / `Ctrl+D` 复制 / `Del` 删除 / 方向键层级），补"Tab 顺序合理、焦点不丢"。
- `prefers-reduced-motion`：动效遵循系统偏好。

---

## 9. 响应式

- ≥1200px：三栏全展开（现状）。
- 900–1200px：右栏折叠为抽屉，画布优先。
- <900px：左栏抽屉化，主推"预览 + 指令栏"（移动端评审场景）。
- 现有 `@media (max-width: 640px)` 断点保留，扩展平板断点。

---

## 10. 落地路线图

| 优先级 | 模块 | 内容 | 状态 |
|---|---|---|---|
| **P0** | 指令栏 | 示例 chips + 未听懂建议条 + 回执四态 + 解读常驻 | ✅ 已实现（痛点 P1/P2） |
| **P0** | 非破坏 | 模板非破坏 + Undo/Redo + 自动保存 + 版本 | ✅ 已实现（P4） |
| **P0** | 画布韧性 | 快照消毒 + 加载回退 + 错误恢复 | ✅ 已实现（P2/P4） |
| **P0** | 精修 | 选中浮动条 + 复制(Ctrl+D) + 外观面板 + 缩放预设 | ✅ 已实现（P3） |
| **P0** | 界面去拥挤 | 顶栏"…"更多菜单 + 平台下拉 + 左侧绘制工具竖栏 | ✅ 已实现 |
| **P1** | 新手引导 | "新手三步"引导卡 + 记住已读 | 🔜 待实现 |
| **P1** | 顶栏分享 | 「分享」主入口 + 分享弹窗（独立预览链接/HTML/PNG） | 🔜 待实现 |
| **P1** | 右侧 Tab | 检查器/变量 两 Tab + 令牌来源徽标 + 深色快捷 | 🔜 待实现 |
| **P1** | 内容编辑 | 数组 props 子项折叠编辑（items/links） | 🔜 待实现 |
| **P2** | 多选 | Shift 框选 + 对齐/分布/成组 | 后续 |
| **P2** | 版本对比 | 版本并排 diff 视图 | 后续 |
| **P2** | 移动端 | <900px 抽屉化 | 后续 |

---

## 11. 附：与现状代码的映射表

| 本方案条目 | 现有实现位置 |
|---|---|
| 选中浮动条 | `client/app.js` `renderSelectionToolbar` |
| 组件复制 Ctrl+D | `src/state.ts` `duplicateComponent` + `src/routes/components.ts` `/api/component/:id/duplicate` |
| 外观面板 | `client/app.js` `renderInspectorProps` 的 appearance 区 |
| 缩放预设 | `client/app.js` `setupZoom` `fitCanvas` |
| 指令回执 | `src/service/design-service.ts` `submitPrompt` + `src/prompt-executor.ts` |
| 解读 | `src/tools/explain-tools.ts` `design_explain_design` + `/api/explain` |
| 画布韧性 | `client/src/canvas-entry.jsx` `sanitizeSnapshot`/`recover` |
| 空状态引导 | `client/app.js` `renderCanvas` 空分支 + `EXAMPLE_PROMPTS` |
| 令牌冲突 | `src/state.ts` `getTokenConflicts` + `client/app.js` `conflict-warnings` |
| 评论/版本 | `src/routes/components.ts` + `src/tools/*` + 左栏二级 Tab |
| 顶栏"…"更多菜单 | `client/index.html` `.more-menu` + `client/app.js` `setupMoreMenu` |
| 平台下拉 | `client/index.html` `#platform-select` + `client/app.js` `setupPlatformSwitcher` |
| 绘制工具竖栏 | `client/index.html` `#canvas-tool-rail` + `client/app.js` `setupDrawTools`/`selectDrawTool` |

---

*本方案是设计蓝图，落地时以"每改动必有测试"为门槛（现有 395 单测 + 6 e2e 冒烟为回归基线）。可按 P0→P1 逐项实施，我随时可以据此进入实现。*
